export enum ProvidersEnum {
  Regex = 'regex',
  Role = 'role',
  Resource = 'resource',
  Scope = 'scope',
  Client = 'client',
  Time = 'time',
  User = 'user',
  ClientScope = 'client-scope',
  Aggregated = 'aggregate',
  Group = 'group',
}
